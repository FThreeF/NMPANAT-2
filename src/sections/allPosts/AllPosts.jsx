import s from './AllPosts.module.scss';

const AllPosts = () => {
    return (
        <section className={s.allPosts}>
            <div className={`${s.container} container`}>
                AllPosts
            </div>
        </section>
    )
}

export default AllPosts;